# 🎨 3D Визуализация буквы Т с преобразованиями и проекциями

Программа для трехмерной визуализации, трансформации и проецирования 3D объектов с акцентом на букву "Т" (первая буква фамилии). Реализованы все основные операции компьютерной графики в интерактивном графическом интерфейсе.

## 📋 Основные возможности

### 🏗️ **Моделирование объектов**
- Построение 3D модели буквы "Т" (каркасная модель)
- Создание вспомогательных объектов (куб, буква "А")
- Динамическое добавление/удаление объектов
- Настройка цвета объектов

### 🔄 **Трехмерные преобразования**
- **Масштабирование** по осям X, Y, Z
- **Перенос** (трансляция) в 3D пространстве
- **Вращение**:
  - Вокруг осей X, Y, Z
  - Вокруг произвольной оси (задается вектором)
- **Матричные преобразования** с отображением итоговой матрицы

### 📐 **Проекции и отображение**
- **Перспективная проекция** (реалистичное отображение)
- **Ортографические проекции** (для чертежей):
  - XY (вид сверху)
  - XZ (вид спереди)
  - YZ (вид сбоку)
- Настраиваемая система координат с осями X, Y, Z
- Отображение координатной сетки

### 💾 **Работа с файлами**
- Сохранение сцены в JSON формате
- Загрузка ранее сохраненных сцен
- Экспорт изображения в EPS формате

## 🏗️ Архитектура программы

### Основные классы:

#### 1. **Vector3D**
- Работа с 3D векторами
- Математические операции (сложение, вычитание, скалярное/векторное произведение)
- Нормализация, вычисление длины

#### 2. **Matrix4x4**
- Матрицы 4×4 для 3D преобразований
- Фабричные методы для создания матриц:
  - Переноса (`translation`)
  - Масштабирования (`scaling`)
  - Вращения вокруг осей (`rotation_x/y/z`)
  - Вращения вокруг произвольной оси (`rotation_axis`)

#### 3. **Object3D**
- Представление 3D объектов
- Хранение вершин, ребер, граней
- Методы для преобразований
- Вычисление ограничивающего параллелепипеда

#### 4. **Letter3DGenerator**
- Генератор 3D объектов
- Заготовки: буква "Т", буква "А", куб

#### 5. **Camera**
- Виртуальная камера
- Матрица вида (view matrix)
- Настройки проекции (FOV, near/far planes)

#### 6. **Canvas3D**
- Основной холст для отображения
- Проекция 3D точек на 2D плоскость
- Отрисовка объектов, осей, сетки

#### 7. **Панели управления:**
- `TransformPanel` - преобразования объектов
- `ObjectPanel` - управление объектами
- `ProjectionPanel` - настройки отображения

## 🚀 Запуск программы

**Запустите файл lab6.exe**